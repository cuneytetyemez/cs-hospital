import React, { Component } from "react";

export default class Appointment extends Component {
  // currentUser = {}


  componentDidMount(){
    //check if user state is set and has value else redirect to login
  }

  render() {
    return (
      <div className="cmp">
      
        <form>

          <legend>Appointment</legend>
        </form>
      
      </div>
    );
  }
}
